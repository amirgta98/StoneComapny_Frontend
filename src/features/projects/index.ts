/**
 * Projects feature.
 *
 * Manages the tenant's completed-project portfolio shown on the
 * storefront (cover image, title and publish time per project).
 */
export { testProjects } from "./data/test-projects";
export type { Project } from "./types";