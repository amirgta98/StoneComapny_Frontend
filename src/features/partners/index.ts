/**
 * Partners feature.
 *
 * Manages the list of companies/clients that have trusted the tenant's
 * stonework, shown as a logo strip on the storefront.
 */
export { testPartners } from "./data/test-partners";
export type { Partner } from "./types";