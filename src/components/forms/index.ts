/**
 * Shared form primitives built on React Hook Form + Zod.
 *
 * Reuse these across product, tenant, category, and settings forms.
 * Do not create separate form implementations per feature.
 */
export {};