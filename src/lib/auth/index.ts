/**
 * Authentication infrastructure.
 *
 * Responsibilities:
 * - login
 * - logout
 * - registration where required
 * - session management
 * - password/account flows
 * - session validation
 *
 * Authentication and authorization are separate concerns.
 * Authorization lives in `lib/permissions`.
 */
export {};