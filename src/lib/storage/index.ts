/**
 * File/image storage infrastructure.
 *
 * This is where Cloudinary / S3-compatible storage integration will live.
 * Production media should never be stored in the Git repository or
 * local application filesystem.
 */
export {};